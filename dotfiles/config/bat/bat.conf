# base16 follows the terminal's 16 colors, so it tracks the Ghostty
# Catppuccin Mocha palette automatically (no separate theme file needed).
--theme="base16"

# Show git modifications, a header, and snip markers for skipped regions
--style="changes,header,snip"

# Use italic text on the terminal (not supported on all terminals)
--italic-text=always
